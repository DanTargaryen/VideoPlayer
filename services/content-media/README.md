# content-media

MS-00 runtime scaffold for content and media capabilities. It currently exposes only `/health/live`, `/health/ready` and `/version`; business APIs, schema, MinIO integration and traffic cutover belong to MS-02.
