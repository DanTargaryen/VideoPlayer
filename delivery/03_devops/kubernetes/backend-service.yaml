apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: video-player
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/part-of: video-player
spec:
  selector:
    app.kubernetes.io/name: backend
  ports:
    - name: http
      port: 3000
      targetPort: http
