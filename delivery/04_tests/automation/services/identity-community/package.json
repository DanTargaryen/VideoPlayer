{
  "name": "@videoplayer/identity-community",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "npm --workspace @videoplayer/shared-contracts run build && npm run prisma:generate",
    "prisma:generate": "prisma generate --schema prisma/schema.prisma",
    "db:migrate": "node prisma/run-db-command.cjs migrate",
    "db:seed": "node prisma/run-db-command.cjs seed",
    "db:test-reset": "node prisma/run-db-command.cjs test-reset",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\" \"test/**/*.ts\"",
    "pretest": "npm --workspace @videoplayer/shared-contracts run build && npm run prisma:generate",
    "test": "vitest run --config vitest.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@prisma/client": "6.9.0",
    "@videoplayer/shared-contracts": "0.1.0",
    "prisma": "6.9.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "eslint": "^9.23.0",
    "typescript": "^5.8.2",
    "vitest": "^4.1.11"
  }
}
