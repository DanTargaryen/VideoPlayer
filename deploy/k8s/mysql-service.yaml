apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: video-player
  labels:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/part-of: video-player
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: mysql
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
