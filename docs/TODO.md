# TODO

## 已对齐 / 已完成

- 已按文档中的技术栈落地为 Vue 3 + TypeScript + Vite 前端、NestJS + TypeScript 后端、Prisma + MySQL 数据层，并保留 MinIO + FFmpeg 媒体处理链路。
- 已补齐分区常量与搜索筛选能力：推荐/娱乐/学习/游戏/科技/直播分区入口、搜索页分区筛选、`latest/hot` 排序、路由参数变化自动刷新。
- 已补齐分发类接口兼容项：`/feeds/categories/:code/videos`、`/feeds/hot`、`/search/hotwords`，并让推荐/搜索只返回已发布视频。
- 已修正互动接口与 API 文档的一致性：点赞/收藏改为 `POST` 新增、`DELETE` 取消，同时前端根据当前状态调用对应接口。
- 已补齐用户中心部分文档要求：创作者数据概览增加粉丝、点赞、收藏、评论统计；稿件支持草稿/驳回后编辑；稿件支持查看审核记录；驳回稿件在违规提醒区展示。
- 已补齐个人资料与用户主页数据一致性：用户表增加 `avatarUrl`、`bio`，`/users/profile` 改为鉴权后持久化更新，用户主页展示头像和简介。

## Next Up / 待实现

- P0：直播链路仍需继续完善为真实可演示闭环，包括 SRS 推流、直播间播放、开播/停播状态落库、直播聊天、直播弹幕、录播生成与回看入口。
- P0：礼物币模块目前仍偏演示数据，需要按文档补齐钱包、每日领取、送礼订单的鉴权与落库，并接入直播间前端交互。
- P0：后台治理需要继续增强筛选与处置能力，包括视频审核状态/时间筛选、举报筛选、评论/弹幕处理备注展示、直播聊天/直播弹幕审核对象接入。
- P1：用户中心还需补齐粉丝趋势、直播管理、直播历史、录播入口，以及更完整的作品编辑能力（替换源文件、替换封面资源、标签编辑）。
- P1：首页与搜索页可继续补充分页、热门榜单展示、直播搜索结果、搜索热词 UI，以及更明确的空状态与错误提示。
- P1：媒体处理链路可继续扩展为多清晰度转码、处理状态字段、失败重试、转码日志和播放资源管理。
- P1：Agent 模块仍为规则/Mock 预览，需要接入真实风险识别结果、保存审核建议，并在管理员视频/文本审核面板中展示。
- P1：补充测试与部署闭环，包括搜索筛选、投稿编辑、审核记录、举报处置、文本审核、直播流程的回归用例，以及 Docker Compose 演示环境说明。
